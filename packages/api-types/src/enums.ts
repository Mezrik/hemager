export enum ContestTypeEnum {
  national = "national",
  international = "international",
}

export enum GenderEnum {
  male = "male",
  female = "female",
  mixed = "mixed",
}

export enum DeploymentCriteria {
  rating = "rating",
  club = "club",
  nationality = "nationality",
}

export enum MatchStateChange {
  matchStart = "matchStart",
  matchEnd = "matchEnd",
  fightStart = "fightStart",
  fightStop = "fightStop",
  pointAdded = "pointAdded",
  pointSubtracted = "pointSubtracted",
}
